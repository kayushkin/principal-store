{
  "name": "@kayushkin/principal-store-types",
  "version": "0.1.0",
  "description": "TypeScript types for principal-store's wire format. Generated from the Go types in the root package — the Go package is the source of truth.",
  "type": "module",
  "types": "./model.ts",
  "exports": { ".": { "types": "./model.ts", "default": "./model.ts" } },
  "files": ["model.ts", "README.md"],
  "license": "Apache-2.0",
  "author": "kayushkin",
  "repository": { "type": "git", "url": "git+https://github.com/kayushkin/principal-store.git", "directory": "ts" }
}
